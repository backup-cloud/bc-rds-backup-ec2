# This file should contain local and secret definitions - copy it to
# secure-definitions.sh and replace the values below with your local
# values.

S3_ACCESS_KEY_ID="{{aws-access-key}}"
S3_SECRET_ACCESS_KEY="{{aws-secret-key}}"
S3_BUCKET="{{s3-bucket}}"
MYSQL_HOST="{{mysql-host}}"
MYSQL_USER="{{mysql-user}}"
MYSQL_PASSWORD="{{mysql-password}}"
SSM_BASE_PATH="{{ssm-base-path}}"
AUTO_TERMINATE=true
